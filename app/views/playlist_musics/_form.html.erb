<%= form_for(@playlist_music) do |f| %>
	<% if @playlist_music.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@playlist_music.errors.count, "error") %> prohibited this playlist_music from being saved:</h2>

			<ul>
				<% @playlist_music.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="field">
		<%= f.label :playlist_id %><br>
		<%= f.number_field :playlist_id %>
	</div>
	
	<div class="field">
		<%= f.label :media_id %><br>
		<%= f.number_field :media_id %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>
