<%= form_for(@medium_comment) do |f| %>
	<% if @medium_comment.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@medium_comment.errors.count, "error") %> prohibited this medium_comment from being saved:</h2>

			<ul>
				<% @medium_comment.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="field">
		<%= f.label :comment_id %><br>
		<%= f.number_field :comment_id %>
	</div>

	<div class="field">
		<%= f.label :medium_id %><br>
		<%= f.number_field :medium_id %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>
