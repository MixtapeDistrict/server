<%= form_tag({:controller => 'collaboration', :action => 'coRequest'}, method: "post") do %>	
	<%= hidden_field_tag(:co_id, @otherID) %>
	<%= submit_tag("Collaborate", :class => 'btn profile-btn btn-primary') %>
<% end %>
