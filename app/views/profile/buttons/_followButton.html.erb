<%= form_tag({:controller => 'profile', :action => 'follow'}, method: "post") do %>	
	<%= hidden_field_tag(:id, @otherID) %>
	<%= submit_tag("Follow", :class => 'btn profile-btn btn-primary') %>
<% end %>
