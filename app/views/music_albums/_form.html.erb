<%= form_for(@music_album) do |f| %>
	<% if @music_album.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@music_album.errors.count, "error") %> prohibited this music_album from being saved:</h2>

			<ul>
				<% @music_album.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="field">
		<%= f.label :media_id %><br>
		<%= f.number_field :media_id %>
	</div>
	<div class="field">
		<%= f.label :album_id %><br>
		<%= f.number_field :album_id %>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>
